spring:
  profiles:
    active: dev
  application:
    name: placement-management

  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

server:
  port: 8080
  servlet:
    context-path: /
